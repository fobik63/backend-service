"use client"

/**
 * @deprecated Prefer `AppHeader` from `@/components/layout`.
 * Kept as a thin alias so existing imports keep working.
 */
export {
  AppHeader as TopBar,
  type AppHeaderProps as TopBarProps,
  type AppHeaderUser as TopBarUser,
} from "@/components/layout/app-header"
