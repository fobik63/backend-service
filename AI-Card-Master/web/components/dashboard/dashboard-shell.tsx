"use client"

/**
 * @deprecated Prefer `AppShell` from `@/components/layout`.
 * Kept as a thin alias so existing imports keep working.
 */
export { AppShell as DashboardShell } from "@/components/layout/app-shell"
